// next.config.js

/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // You can add more configurations here as needed
  transpilePackages: ["@cardynale/ui"],
};

module.exports = nextConfig;
